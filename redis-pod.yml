apiVersion: v1
kind: Pod
metadata:
 name: redis-app-pod
 labels:
  name: redis-pod
  app: demo-voting-app
spec:
 conatiners:
  - name: redis
    image: redis
    port: 
     -conatainerPort: 6379